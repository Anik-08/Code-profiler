# Core data & ML
numpy>=1.24.0,<2.0
pandas>=1.5.0,<2.0
scikit-learn>=1.2.0,<2.0
lightgbm>=3.3.0,<4.0
joblib>=1.2.0,<2.0

# System/process measurement
psutil>=5.9.0,<6.0

# Optional: RAPL-based energy measurement (Linux only)
# Install on Linux if you will measure energy via RAPL; it's optional.
pyRAPL; sys_platform == "linux"

# ONNX conversion / inference (optional; used for exporting / running model locally)
skl2onnx>=1.16.0,<2.0
onnxmltools>=1.6.0,<2.0
onnx>=1.14.0,<2.0
onnxruntime>=1.15.0,<2.0

# Server & API (the ml/ server_model_api.py uses FastAPI/uvicorn)
fastapi>=0.95.0,<1.0
uvicorn[standard]>=0.22.0,<1.1
pydantic>=1.10.0,<2.0